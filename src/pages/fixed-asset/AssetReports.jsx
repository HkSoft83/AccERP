import React from 'react';import { Button } from '@/components/ui/button';const AssetReports = ({ assets }) => {  const downloadCSV = (data, filename) => {    const csvContent = "data:text/csv;charset=utf-8," +      Object.keys(data[0]).join(",") + "\n" +      data.map(e => Object.values(e).join(",")).join("\n");    const link = document.createElement("a");    link.setAttribute("href", encodeURI(csvContent));    link.setAttribute("download", filename);    document.body.appendChild(link);    link.click();    document.body.removeChild(link);  };  const handleDownloadRegister = () => {    downloadCSV(assets, "fixed-asset-register.csv");  };  const handleDownloadDepreciationSchedule = () => {    const depreciationSchedule = assets.map(asset => {      const originalCost = parseFloat(asset.amount) || 0;      const salvageValue = parseFloat(asset.salvageValue) || 0;      const usefulLife = parseInt(asset.usefulLife, 10) || 1;      const monthlyDepreciation = (originalCost - salvageValue) / (usefulLife * 12);      return {        'Asset Name': asset.name,        'Original Cost': originalCost.toFixed(2),        'Salvage Value': salvageValue.toFixed(2),        'Useful Life (Years)': usefulLife,        'Monthly Depreciation': monthlyDepreciation.toFixed(2),      };    });    downloadCSV(depreciationSchedule, "depreciation-schedule.csv");  };  return (    <div className="p-4 border rounded-lg">      <h2 className="text-xl font-bold mb-4">Asset Reports</h2>      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">        <Button variant="outline" onClick={handleDownloadRegister}>Fixed Asset Register</Button>        <Button variant="outline" onClick={handleDownloadDepreciationSchedule}>Depreciation Schedule</Button>        <Button variant="outline">Asset Movement Report</Button>        <Button variant="outline">Disposal Report</Button>        <Button variant="outline">Revaluation Report</Button>        <Button variant="outline">Repair & Maintenance History</Button>      </div>    </div>  );};export default AssetReports;
