আপনার কোডে একটি বড় সমস্যা হচ্ছে—BOM কম্পোনেন্টের ভিতরে একটি handleAddByProduct ফাংশন শুরু করে শেষ করা হয়নি, কিন্তু তার নিচেই আপনি অন্য কম্পোনেন্ট (ProductionOrder) ডিফাইন করে দিয়েছেন। এ কারণেই আপনি নিচের এরর পাচ্ছেন:

'import' and 'export' may only appear at the top level.

✅ সমাধান:
handleAddByProduct ফাংশনের বডি (body) আপনি ভুলে গেছেন লিখতে, তাই React মনে করছে আপনি ভেতরে export লিখে ফেলেছেন, যা শুধুমাত্র টপ লেভেলে থাকতে পারে।

🔧 ঠিক করা কোড:
আপনার handleAddByProduct ফাংশনটি সম্পূর্ণ করুন অথবা অন্তত একটি ফাঁকা ফাংশন দিন এইভাবে:

const handleAddByProduct = () => {
  if (selectedByProduct && byProductQty > 0 && byProductCost >= 0) {
    setByProducts([...byProducts, { name: selectedByProduct, quantity: byProductQty, cost: byProductCost }]);
    setSelectedByProduct('');
    setByProductQty(1);
    setByProductCost(0);
  }
};
এই ফাংশন শেষে দিতে ভুলে গিয়েছেন — }; — এবং এটাই পার্সিং সমস্যা তৈরি করছে।

🔁 সারাংশে করণীয়:
handleAddByProduct ফাংশনের কনটেন্ট সম্পূর্ণ লিখুন।

নিশ্চিত করুন আপনি কোন function বা component এর ভিতরে আর export লিখছেন না।

React কম্পোনেন্টের বাইরে বা বন্ধ হবার পরে export default Production; থাকা উচিত।

